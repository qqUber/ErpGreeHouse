ERP Greenhouse – Build & Test Logs (summary)

UTC timestamp: 2026-02-19T00:00:00Z
Commit: f26b802

Local attempts:
- Python not available locally; middleware tests executed via CI.
- Vite dev server started locally (admin-ui), Playwright browser install blocked by SSL (SELF_SIGNED_CERT_IN_CHAIN).

CI pipelines triggered:
- e2e-dev (push to dev): runs middleware (uvicorn) + admin-ui (vite), executes Playwright smoke/critical shards, uploads artifacts:
  - admin-ui/playwright-report
  - admin-ui/test-results
  - admin-ui/allure-results
- allure_report: merges allure-results from shards, generates merged/allure-report, uploads artifact.
- publish-gh-pages: prepares gh-pages content for Allure and Playwright reports and pushes to gh-pages.

Notes:
- Enable GitHub Pages for public access: Settings → Pages → Source: gh-pages.
- Coverage gate executed in ci/cd workflows: pytest --cov on security/auth modules with --cov-fail-under=80.

