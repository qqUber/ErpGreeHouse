---
phase: 02-loyalty-program
plan: 02
type: test
---

<objective>
Enhance test coverage for loyalty-related code. The existing tests cover core functionality, but additional tests are needed for Redis-based operations, API endpoints, and edge cases.

**Areas needing additional test coverage:**
- Redis-based leaderboard and tier calculations
- TMA API endpoints for loyalty
- Integrations API for POS receipt processing
- Edge cases and boundary conditions
- Error handling scenarios

Purpose: Improve test coverage to ensure robustness and maintainability of the loyalty program.
Output: Enhanced test coverage with new test files and passing tests.
</objective>

<context>
@C:\Users\AASS\IdeaProjects\ErpGreeHouse\middleware\app\loyalty.py
@C:\Users\AASS\IdeaProjects\ErpGreeHouse\middleware\app\tma_api.py
@C:\Users\AASS\IdeaProjects\ErpGreeHouse\middleware\app\integrations_api.py
@C:\Users\AASS\IdeaProjects\ErpGreeHouse\middleware\tests\unit\test_loyalty_core.py
</context>

<tasks>

<task type="auto">
  <name>Create tests for Redis-based operations</name>
  <action>
    1. Create a new test file for Redis-based loyalty operations
    2. Write tests for:
       - update_user_spent_score
       - increment_user_spent_score
       - get_user_rank
       - get_user_percentile
       - get_top_spenders
       - get_tier_from_percentile
       - get_users_in_tier
       - batch_update_scores
       - get_user_score
       - remove_user_from_leaderboard
       - get_leaderboard_count
       - get_rank_range
       - calculate_user_tier
       - get_tier_leaderboard
       - get_user_tier_rank
       - bulk_increment_scores
    3. Use mocking to isolate Redis dependency
  </action>
  <files>middleware/tests/unit/test_loyalty_redis.py</files>
  <verify>Test file created with comprehensive coverage</verify>
  <done>Redis-based operations tests created</done>
</task>

<task type="auto">
  <name>Create tests for TMA API endpoints</name>
  <action>
    1. Create a new test file for TMA API endpoints
    2. Write tests for:
       - get_tma_me endpoint
       - validate_init_data function
    3. Use FastAPI test client and mocking
  </action>
  <files>middleware/tests/unit/test_tma_api.py</files>
  <verify>Test file created with API endpoint tests</verify>
  <done>TMA API tests created</done>
</task>

<task type="auto">
  <name>Create tests for integrations API</name>
  <action>
    1. Create a new test file for integrations API endpoints
    2. Write tests for:
       - ingest_pos_receipt endpoint
       - loyalty points calculations in receipt processing
       - customer lookup and creation
    3. Test edge cases for receipt processing
  </action>
  <files>middleware/tests/unit/test_integrations_api.py</files>
  <verify>Test file created with integrations API tests</verify>
  <done>Integrations API tests created</done>
</task>

<task type="auto">
  <name>Create tests for advanced loyalty scenarios</name>
  <action>
    1. Create a new test file for advanced loyalty scenarios
    2. Write tests for:
       - Edge cases in points calculation
       - Tier boundary conditions
       - Error handling scenarios
       - Redis unavailability handling
    3. Test with custom LoyaltyRules configurations
  </action>
  <files>middleware/tests/unit/test_loyalty_advanced.py</files>
  <verify>Test file created with advanced scenario tests</verify>
  <done>Advanced loyalty scenarios tests created</done>
</task>

<task type="auto">
  <name>Run all loyalty-related tests</name>
  <action>
    1. Run all loyalty-related tests to ensure everything passes
    2. Execute: `cd middleware && python -m pytest tests/unit/test_loyalty*.py tests/unit/test_tma_api.py tests/unit/test_integrations_api.py -v`
    3. Check for any failures or errors
  </action>
  <verify>All tests pass successfully</verify>
  <done>All loyalty tests executed and passed</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Enhanced loyalty program test coverage</what-built>
  <how-to-verify>
    1. Check that all new test files are created
    2. Review the test execution results
    3. Verify that all tests (existing and new) pass
    4. Check coverage report if available
  </how-to-verify>
  <resume-signal>Type "approved" if all tests passed, or describe any failures</resume-signal>
</task>

</tasks>

<verification>
pytest command to verify all loyalty tests:
`cd middleware && python -m pytest tests/unit/test_loyalty*.py tests/unit/test_tma_api.py tests/unit/test_integrations_api.py -v`
</verification>

<success_criteria>

- All existing loyalty tests continue to pass
- All new tests created pass
- Tests cover Redis-based operations, API endpoints, and advanced scenarios
- No failures or errors in test execution
  </success_criteria>

<output>
After completion, create SUMMARY.md with:
- Number of new tests added
- Coverage improvements
- Test execution results
- Any failures or errors encountered
- Test files created/modified
</output>
