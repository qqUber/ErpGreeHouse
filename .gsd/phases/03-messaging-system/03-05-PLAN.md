---
phase: 03-messaging-system
plan: 05
wave: 3
autonomous: false
gap_closure: false
---

<objective>
Enhance customer segmentation and trigger system. Add advanced segmentation criteria, dynamic segments, and additional trigger types. Improve the segment builder UI for better user experience.
Purpose: Enable more targeted and effective marketing campaigns by providing advanced segmentation capabilities.
Output: Enhanced segmentation and trigger system with improved UI.
</objective>

<context>
@.gsd/phases/03-messaging-system/03-RESEARCH.md
@.gsd/phases/03-messaging-system/03-CONTEXT.md
@middleware/app/marketing_api.py
@middleware/app/trigger_engine.py
@admin-ui/src/MarketingView.tsx
</context>

<must_haves>

- [ ] Advanced segmentation criteria (purchase history, preferences, demographics)
- [ ] Dynamic segment evaluation
- [ ] Additional trigger types
- [ ] Improved segment builder UI
- [ ] Segment preview functionality
- [ ] Segment performance metrics

</must_haves>

<should_haves>

- [ ] Custom segment criteria
- [ ] Segment comparison
- [ ] Segment overlap analysis
- [ ] A/B testing for segments

</should_haves>

<tasks>

<task type="auto">
  <name>Add advanced segmentation criteria</name>
  <files>middleware/app/marketing_api.py</files>
  <action>
    1. Extend segment criteria with purchase history filters
    2. Add preferences and demographics criteria
    3. Implement segment evaluation for new criteria
    4. Add segment preview endpoint
  </action>
  <verify>
    - New criteria can be added to segments
    - Segments with new criteria are evaluated correctly
    - Preview endpoint returns matching customers
  </verify>
  <done>Advanced segmentation criteria added</done>
</task>

<task type="auto">
  <name>Implement dynamic segment evaluation</name>
  <files>middleware/app/marketing_api.py</files>
  <action>
    1. Create background task for dynamic segment updates
    2. Implement segment recalculation on customer data changes
    3. Add segment refresh API endpoint
    4. Add last updated timestamp to segments
  </action>
  <verify>
    - Segments are recalculated dynamically
    - Refresh endpoint works
    - Last updated timestamps are accurate
  </verify>
  <done>Dynamic segment evaluation implemented</done>
</task>

<task type="auto">
  <name>Add additional trigger types</name>
  <files>middleware/app/trigger_engine.py, middleware/app/worker.py</files>
  <action>
    1. Implement points expiration trigger
    2. Add purchase follow-up trigger
    3. Create welcome message trigger
    4. Extend trigger engine to support new types
  </action>
  <verify>
    - New trigger types are available
    - Triggers are evaluated correctly
    - Messages are sent when triggers fire
  </verify>
  <done>Additional trigger types added</done>
</task>

<task type="auto">
  <name>Improve segment builder UI</name>
  <files>admin-ui/src/MarketingView.tsx</files>
  <action>
    1. Redesign segment creation interface
    2. Add visual criteria builder
    3. Implement segment preview
    4. Add criteria validation
  </action>
  <verify>
    - UI is intuitive and easy to use
    - Preview functionality works
    - Validation errors are shown
  </verify>
  <done>Segment builder UI improved</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Enhanced segmentation and trigger system</what-built>
  <how-to-verify>
    1. Visit Admin UI Marketing section → Segments
    2. Create a new segment with advanced criteria (e.g., purchase history, preferences)
    3. Preview the segment to see matching customers
    4. Create a campaign using this segment
    5. Create a new trigger with one of the new trigger types
    6. Verify that the segment is dynamically updated
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>

Run the following commands to verify:

```bash
cd middleware
python -m pytest tests/unit/ -v -k "segment"
```

Expected output:
- New segmentation criteria are available
- Segments with new criteria are evaluated correctly
- Preview endpoint returns matching customers
- New trigger types are available

</verification>

<success_criteria>

- [ ] All tasks executed
- [ ] Each task committed individually
- [ ] Advanced segmentation criteria are available
- [ ] Dynamic segment evaluation works
- [ ] New trigger types are implemented
- [ ] UI is improved and user-friendly

</success_criteria>

<output>
After completion, create SUMMARY.md with:
- New segmentation criteria added
- Dynamic segment evaluation implementation
- New trigger types implemented
- UI improvements
- Any notable deviations or issues
- Verification check results
</output>
