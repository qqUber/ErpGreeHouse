---
phase: 03-messaging-system
plan: 04
wave: 2
autonomous: true
gap_closure: false
---

<objective>
Implement VK integration for messaging system. Create VK bot handler, message sending functions, and integrate with existing campaign management system.
Purpose: Enable omnichannel messaging across Telegram, VK, and mobile app for maximum customer reach.
Output: Working VK integration with message sending capabilities.
</objective>

<context>
@.gsd/phases/03-messaging-system/03-RESEARCH.md
@.gsd/phases/03-messaging-system/03-CONTEXT.md
@middleware/app/integrations/bots/vk_handler.py
@middleware/app/worker.py
@middleware/app/marketing_api.py
@admin-ui/src/MarketingView.tsx
</context>

<must_haves>

- [ ] VK bot handler implementation
- [ ] VK message sending worker functions
- [ ] VK API configuration
- [ ] Integration with campaign management
- [ ] VK media message support
- [ ] Error handling for VK API calls

</must_haves>

<should_haves>

- [ ] VK user profile synchronization
- [ ] VK chat management
- [ ] VK message templates
- [ ] VK campaign analytics

</should_haves>

<tasks>

<task type="auto">
  <name>Create VK bot handler</name>
  <files>middleware/app/integrations/bots/vk_handler.py</files>
  <action>
    1. Implement VK API client
    2. Create message sending functions
    3. Implement media upload functionality
    4. Add error handling for VK API calls
  </action>
  <verify>
    - VK handler initializes correctly
    - Text messages can be sent
    - Media messages can be sent
    - Errors are properly handled
  </verify>
  <done>VK bot handler created</done>
</task>

<task type="auto">
  <name>Create VK message sending worker</name>
  <files>middleware/app/worker.py</files>
  <action>
    1. Add VK message sending task to Celery
    2. Implement rate limiting for VK API
    3. Add delivery tracking for VK messages
    4. Create retry logic for failed attempts
  </action>
  <verify>
    - VK message tasks are queued
    - Rate limits are respected
    - Delivery events are tracked
  </verify>
  <done>VK message sending worker created</done>
</task>

<task type="auto">
  <name>Add VK configuration</name>
  <files>middleware/app/config.py</files>
  <action>
    1. Add VK API configuration to Settings class
    2. Add VK bot token and group ID settings
    3. Configure VK API version and endpoints
    4. Add VK rate limit configuration
  </action>
  <verify>
    - Configuration is correctly loaded from environment
    - Settings are accessible to handlers
  </verify>
  <done>VK configuration added</done>
</task>

<task type="auto">
  <name>Integrate VK with campaign management</name>
  <files>middleware/app/marketing_api.py</files>
  <action>
    1. Extend campaign creation to support VK channel
    2. Modify send_campaign endpoint to include VK
    3. Add VK-specific customer segmentation
    4. Update campaign analytics to include VK metrics
  </action>
  <verify>
    - VK channel can be selected for campaigns
    - VK messages are sent to targeted customers
    - Campaign analytics include VK data
  </verify>
  <done>VK integration with campaign management completed</done>
</task>

<task type="auto">
  <name>Update Admin UI for VK integration</name>
  <files>admin-ui/src/MarketingView.tsx, admin-ui/src/api.ts</files>
  <action>
    1. Add VK channel option to campaign creation
    2. Update channel selection UI
    3. Add VK message preview
    4. Include VK metrics in analytics
  </action>
  <verify>
    - VK channel is selectable in Admin UI
    - Message preview works for VK
    - VK analytics are displayed
  </verify>
  <done>Admin UI updated for VK integration</done>
</task>

</tasks>

<verification>

Run the following commands to verify:

```bash
cd middleware
python -m pytest tests/unit/ -v -k "vk"
```

Expected output:
- VK handler initializes with token
- Text messages can be sent
- Media messages can be sent
- VK channel is available in campaign creation

</verification>

<success_criteria>

- [ ] All tasks executed
- [ ] Each task committed individually
- [ ] VK bot handler is functional
- [ ] VK messages can be sent via campaigns
- [ ] Admin UI supports VK channel selection
- [ ] VK analytics are tracked

</success_criteria>

<output>
After completion, create SUMMARY.md with:
- VK handler implementation details
- Worker integration changes
- Configuration settings
- API endpoint changes
- Admin UI updates
- VK channel analytics
- Any notable deviations or issues
</output>
