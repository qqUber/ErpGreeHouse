---
phase: 03-messaging-system
plan: 02
wave: 1
autonomous: true
gap_closure: false
---

<objective>
Implement comprehensive rate limiting to prevent channel bans. Create per-channel rate limiting with Redis token bucket algorithm and integrate with existing messaging system.
Purpose: Ensure the messaging system complies with API rate limits to avoid temporary or permanent bans from Telegram and VK platforms.
Output: Working rate limiting system that prevents API rate limit violations.
</objective>

<context>
@.gsd/phases/03-messaging-system/03-RESEARCH.md
@.gsd/phases/03-messaging-system/03-CONTEXT.md
@middleware/app/worker.py
@middleware/app/config.py
</context>

<must_haves>

- [ ] Redis-based rate limiter implementation
- [ ] Per-channel rate limit configuration
- [ ] Integration with message sending workers
- [ ] Error handling for rate limit violations
- [ ] Retry logic for failed delivery attempts
- [ ] Monitoring of rate limit usage

</must_haves>

<should_haves>

- [ ] Dynamic rate limit adjustment
- [ ] Rate limit analytics dashboard
- [ ] Alerting for rate limit threshold breaches

</should_haves>

<tasks>

<task type="auto">
  <name>Install rate limiting library</name>
  <files>middleware/requirements.txt</files>
  <action>
    1. Add limited-aiogram library to requirements.txt
    2. Install the library
  </action>
  <verify>
    - Library is installed successfully
    - Can import limited_aiogram module
  </verify>
  <done>Rate limiting library installed</done>
</task>

<task type="auto">
  <name>Create rate limiter module</name>
  <files>middleware/app/rate_limiter.py</files>
  <action>
    1. Implement Redis-based token bucket algorithm
    2. Add per-channel rate limit configuration
    3. Create rate limit check function
    4. Implement retry logic for rate limit violations
  </action>
  <verify>
    - Rate limiter initializes correctly
    - Rate limit checks work
    - Token bucket refills properly
  </verify>
  <done>Rate limiter module created</done>
</task>

<task type="auto">
  <name>Integrate rate limiter with workers</name>
  <files>middleware/app/worker.py</files>
  <action>
    1. Patch aiogram Bot class with limited_aiogram
    2. Add rate limit checks before sending messages
    3. Implement retry logic for failed attempts
    4. Add logging for rate limit violations
  </action>
  <verify>
    - Worker functions respect rate limits
    - Retry logic works correctly
    - Errors are properly logged
  </verify>
  <done>Rate limiter integrated with workers</done>
</task>

<task type="auto">
  <name>Configure per-channel rate limits</name>
  <files>middleware/app/config.py</files>
  <action>
    1. Add rate limit configuration to Settings class
    2. Configure Telegram rate limits (30 msg/sec, 1 msg/sec per chat)
    3. Configure VK rate limits (20 msg/min per group, 1 msg/sec per user)
    4. Add mobile app push notification limits (100 msg/sec)
  </action>
  <verify>
    - Configuration is correctly loaded
    - Rate limits can be customized
  </verify>
  <done>Per-channel rate limits configured</done>
</task>

<task type="auto">
  <name>Add rate limit monitoring</name>
  <files>middleware/app/marketing_api.py</files>
  <action>
    1. Create endpoint for rate limit status
    2. Add rate limit metrics to campaign analytics
    3. Implement rate limit usage tracking
  </action>
  <verify>
    - Rate limit status endpoint returns correct data
    - Analytics include rate limit metrics
  </verify>
  <done>Rate limit monitoring implemented</done>
</task>

</tasks>

<verification>

Run the following commands to verify:

```bash
cd middleware
python -m pytest tests/unit/ -v -k "rate"
```

Expected output:
- Rate limiter initializes with Redis
- Rate limit checks return correct boolean values
- Retry logic works for rate limit violations
- Worker functions respect rate limits

</verification>

<success_criteria>

- [ ] All tasks executed
- [ ] Each task committed individually
- [ ] Rate limiting is correctly applied per channel
- [ ] Retry logic works for failed attempts
- [ ] Rate limit violations are properly handled

</success_criteria>

<output>
After completion, create SUMMARY.md with:
- Rate limiter implementation details
- Per-channel rate limit configuration
- Worker integration changes
- Monitoring and analytics endpoints
- Any notable deviations or issues
</output>
