---
phase: 03-messaging-system
plan: 01
wave: 1
autonomous: true
gap_closure: false
---

<objective>
Implement media message support for the messaging system. Add support for photos, videos, documents, and media groups (albums) for Telegram. Extend the database schema and API to handle media content.
Purpose: Enable rich media messages to improve customer engagement with promotional content, product images, and video offers.
Output: Working media message functionality with API endpoints and Admin UI support.
</objective>

<context>
@.gsd/phases/03-messaging-system/03-RESEARCH.md
@.gsd/phases/03-messaging-system/03-CONTEXT.md
@middleware/app/marketing_api.py
@middleware/app/worker.py
@middleware/app/db.py
@admin-ui/src/MarketingView.tsx
</context>

<must_haves>

- [ ] Database schema extensions for media fields
- [ ] API endpoints for creating media messages
- [ ] Worker functions for sending media messages
- [ ] Admin UI for media message creation
- [ ] Support for photos, videos, documents, and media groups
- [ ] File validation and storage
- [ ] Error handling for media sending failures

</must_haves>

<should_haves>

- [ ] Media template variables
- [ ] Media preview in Admin UI
- [ ] File size validation
- [ ] Media type validation

</should_haves>

<tasks>

<task type="auto">
  <name>Extend database schema for media fields</name>
  <files>middleware/app/db.py</files>
  <action>
    1. Add media fields to marketing_campaigns table: content_type, media_urls, caption
    2. Add media fields to marketing_triggers table: media_type, media_url, caption  
    3. Extend marketing_events table with event_data field
    4. Extend marketing_trigger_events table with delivery tracking fields
  </action>
  <verify>
    - Database migrations run successfully
    - New fields are nullable for backward compatibility
  </verify>
  <done>Database schema extended to support media messages</done>
</task>

<task type="auto">
  <name>Create media message API endpoints</name>
  <files>middleware/app/marketing_api.py</files>
  <action>
    1. Extend CampaignCreate schema with media fields
    2. Extend TriggerCreate schema with media fields
    3. Add media upload endpoint for file storage
    4. Modify send_campaign endpoint to handle media messages
  </action>
  <verify>
    - API endpoints return correct status codes
    - Media files are properly uploaded and stored
    - Existing text message functionality still works
  </verify>
  <done>API endpoints for media messages created</done>
</task>

<task type="auto">
  <name>Implement media sending worker functions</name>
  <files>middleware/app/worker.py</files>
  <action>
    1. Create safe_send_photo function
    2. Create safe_send_video function
    3. Create safe_send_document function
    4. Create send_media_group function
    5. Extend send_campaign_message task to handle media types
  </action>
  <verify>
    - Worker functions send media messages correctly
    - Error handling works for failed media sending
    - Tasks are properly queued and executed
  </verify>
  <done>Media sending worker functions implemented</done>
</task>

<task type="auto">
  <name>Update Admin UI for media messages</name>
  <files>admin-ui/src/MarketingView.tsx, admin-ui/src/api.ts</files>
  <action>
    1. Extend API types for media fields
    2. Update CampaignsManager to handle media content
    3. Add media upload interface
    4. Update message preview to handle media
  </action>
  <verify>
    - Admin UI renders media message form correctly
    - Media files can be uploaded
    - Campaign creation works with media content
  </verify>
  <done>Admin UI for media messages updated</done>
</task>

<task type="auto">
  <name>Implement file validation and storage</name>
  <files>middleware/app/storage.py</files>
  <action>
    1. Create media storage service
    2. Implement file size validation
    3. Implement media type validation
    4. Create file upload and retrieval endpoints
  </action>
  <verify>
    - Files are validated before upload
    - Invalid files are rejected
    - Files are stored in appropriate location
  </verify>
  <done>File validation and storage implemented</done>
</task>

</tasks>

<verification>

Run the following commands to verify:

```bash
cd middleware
python -m pytest tests/unit/ -v -k "media"
```

Expected output:
- Media message creation endpoints return 200
- Worker tasks for media sending are queued
- Database entries include media fields
- Admin UI renders media upload form

</verification>

<success_criteria>

- [ ] All tasks executed
- [ ] Each task committed individually
- [ ] Media messages can be created and sent
- [ ] Admin UI supports media upload
- [ ] Existing functionality remains intact

</success_criteria>

<output>
After completion, create SUMMARY.md with:
- Database schema changes
- API endpoint changes
- Worker function changes
- Admin UI changes
- File storage configuration
- Any notable deviations or issues
</output>
