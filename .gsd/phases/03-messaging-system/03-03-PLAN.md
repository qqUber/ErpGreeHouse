---
phase: 03-messaging-system
plan: 03
wave: 2
autonomous: true
gap_closure: false
---

<objective>
Implement message delivery and open rate tracking. Extend the database schema to track sent, delivered, opened, and clicked events. Create analytics endpoints and Admin UI for campaign performance tracking.
Purpose: Provide insights into message performance to optimize marketing campaigns and improve customer engagement.
Output: Working delivery tracking system with analytics dashboard.
</objective>

<context>
@.gsd/phases/03-messaging-system/03-RESEARCH.md
@.gsd/phases/03-messaging-system/03-CONTEXT.md
@middleware/app/marketing_api.py
@middleware/app/worker.py
@middleware/app/db.py
@admin-ui/src/MarketingView.tsx
@admin-ui/src/components/AnalyticsCharts.tsx
</context>

<must_haves>

- [ ] Database schema for tracking events
- [ ] Event tracking in worker functions
- [ ] Analytics API endpoints
- [ ] Admin UI analytics dashboard
- [ ] Click tracking with shortened URLs
- [ ] Campaign performance metrics

</must_haves>

<should_haves>

- [ ] Real-time analytics updates
- [ ] A/B testing metrics
- [ ] Funnel analysis (sent → delivered → opened → clicked)
- [ ] Export functionality for analytics data

</should_haves>

<tasks>

<task type="auto">
  <name>Extend database for event tracking</name>
  <files>middleware/app/db.py</files>
  <action>
    1. Extend marketing_events table with event_data column
    2. Add delivered_at, opened_at, clicked_at to marketing_trigger_events
    3. Create indexes for fast querying
  </action>
  <verify>
    - Database migrations run successfully
    - New fields are nullable for backward compatibility
  </verify>
  <done>Database schema extended for event tracking</done>
</task>

<task type="auto">
  <name>Implement event tracking in workers</name>
  <files>middleware/app/worker.py</files>
  <action>
    1. Add sent event tracking when message is queued
    2. Add delivered event tracking when message is sent successfully
    3. Add opened event tracking (simulated)
    4. Add clicked event tracking with URL parameters
  </action>
  <verify>
    - Events are properly recorded in database
    - Event data contains relevant metadata
  </verify>
  <done>Event tracking in workers implemented</done>
</task>

<task type="auto">
  <name>Create analytics API endpoints</name>
  <files>middleware/app/marketing_api.py</files>
  <action>
    1. Create endpoint for campaign performance metrics
    2. Create endpoint for event breakdown by type
    3. Create endpoint for channel-specific analytics
    4. Add export endpoint for CSV reports
  </action>
  <verify>
    - Analytics endpoints return correct data
    - Metrics are calculated correctly
    - CSV export works
  </verify>
  <done>Analytics API endpoints created</done>
</task>

<task type="auto">
  <name>Update Admin UI analytics</name>
  <files>admin-ui/src/components/AnalyticsCharts.tsx</files>
  <action>
    1. Add campaign performance dashboard
    2. Create event breakdown charts
    3. Add channel comparison charts
    4. Implement date range filters
  </action>
  <verify>
    - Analytics charts render correctly
    - Data updates with filters
    - Charts are interactive
  </verify>
  <done>Admin UI analytics updated</done>
</task>

<task type="auto">
  <name>Implement URL shortening and tracking</name>
  <files>middleware/app/url_shortener.py</files>
  <action>
    1. Create URL shortener module
    2. Implement click tracking middleware
    3. Add tracking parameters to links
    4. Create endpoint for tracking clicks
  </action>
  <verify>
    - URLs are properly shortened
    - Click events are tracked
    - Click data is associated with campaigns
  </verify>
  <done>URL shortening and tracking implemented</done>
</task>

</tasks>

<verification>

Run the following commands to verify:

```bash
cd middleware
python -m pytest tests/unit/ -v -k "tracking"
```

Expected output:
- Event tracking endpoints return 200
- Events are recorded in database
- Analytics charts render with data
- URL shortening and tracking work

</verification>

<success_criteria>

- [ ] All tasks executed
- [ ] Each task committed individually
- [ ] Event tracking works for all message types
- [ ] Analytics dashboard is functional
- [ ] URL shortening and click tracking work

</success_criteria>

<output>
After completion, create SUMMARY.md with:
- Event tracking implementation details
- Analytics API endpoint changes
- Admin UI analytics changes
- URL shortening and tracking details
- Campaign performance metrics
- Any notable deviations or issues
</output>
