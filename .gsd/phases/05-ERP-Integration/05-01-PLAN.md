---
phase: 05-ERP-integration
plan: 01
wave: 1
depends_on: [04-04]
files_modified:
  - middleware/app/integrations/erpnext.py
  - middleware/app/integrations/erp_sync.py
  - middleware/app/integrations/webhooks.py
  - middleware/app/models/erp_sync.py
autonomous: true
---

<objective>
Implement ERPNext integration for customer data synchronization, purchase data import, and loyalty program data export.

Key features:
- Bidirectional customer data synchronization
- Purchase data import for loyalty point calculation
- Loyalty program data export for reporting
- Event-driven architecture with polling fallback
- Circuit breaker for API resilience
- Dead letter queue for failed events
- Metrics collection and monitoring
</objective>

<context>
@.gsd/STATE.md
@.gsd/ROADMAP.md
@.gsd/phases/05-ERP-integration/05-CONTEXT.md
@.gsd/phases/05-ERP-integration/05-RESEARCH.md
</context>

<must_haves>
- ERP-001: Customer data synchronized with ERPNext
- ERP-002: Purchase data imported from ERP for loyalty point calculation
- ERP-003: Loyalty program data exported to ERP
- Integration architecture handles errors gracefully
- Data sync processes are automated and scheduled
</must_haves>

<tasks>
  <task type="auto" id="1">
    <name>Create ERPNext API client module</name>
    <files>middleware/app/integrations/erpnext.py</files>
    <action>
      Implement ERPNext API client using frappe-client library:
      - Token-based authentication
      - Methods for fetching customers
      - Methods for fetching sales invoices (purchases)
      - Methods for updating customer loyalty data
      - Handle rate limits and errors
    </action>
    <verify>
      - File exists at expected path
      - Imports frappe-client library
      - Contains methods: get_customers(), get_sales_invoices(), update_customer_loyalty()
      - Authentication logic implemented
    </verify>
    <done>ERPNext API client module created</done>
  </task>

  <task type="auto" id="2">
    <name>Implement ERP data synchronization service</name>
    <files>middleware/app/integrations/erp_sync.py</files>
    <action>
      Create ERP sync service with:
      - Customer sync (bidirectional)
      - Purchase import from sales invoices
      - Loyalty data export
      - Polling for missed events
      - Circuit breaker pattern
      - Dead letter queue for failed events
    </action>
    <verify>
      - File exists at expected path
      - Imports erpnext.py client
      - Contains sync methods: sync_customers(), import_purchases(), export_loyalty_data()
      - Circuit breaker decorators applied
      - DLQ logic implemented
    </verify>
    <done>ERP data synchronization service implemented</done>
  </task>

  <task type="auto" id="3">
    <name>Create ERPNext webhook handler</name>
    <files>middleware/app/integrations/webhooks.py</files>
    <action>
      Implement FastAPI endpoint for ERPNext webhooks:
      - Handle customer created/updated/deleted events
      - Handle sales invoice created events
      - Trigger sync operations on webhook events
      - Validate webhook signatures
    </action>
    <verify>
      - File exists at expected path
      - FastAPI router with POST endpoint for webhooks
      - Event handlers for customer and sales invoice events
      - Signature validation logic
    </verify>
    <done>ERPNext webhook handler created</done>
  </task>

  <task type="auto" id="4">
    <name>Create ERP sync database models</name>
    <files>middleware/app/models/erp_sync.py</files>
    <action>
      Define database models for sync tracking:
      - Sync history
      - Failed events (DLQ)
      - Sync metrics
      - Include timestamps and event details
    </action>
    <verify>
      - File exists at expected path
      - Defines SQLAlchemy models: SyncHistory, FailedEvent, SyncMetric
      - Fields for tracking sync operations
    </verify>
    <done>ERP sync database models created</done>
  </task>

  <task type="auto" id="5">
    <name>Set up APScheduler for periodic sync tasks</name>
    <files>middleware/app/main.py</files>
    <action>
      Configure APScheduler for:
      - 15-minute purchase import
      - Hourly customer sync
      - Daily loyalty export
      - Job persistence and error handling
    </action>
    <verify>
      - Scheduler initialized in main.py
      - Jobs for purchase import, customer sync, loyalty export
      - Job triggers configured correctly
    </verify>
    <done>APScheduler set up for periodic sync tasks</done>
  </task>

  <task type="auto" id="6">
    <name>Implement Prometheus metrics collection</name>
    <files>middleware/app/integrations/erp_sync.py</files>
    <action>
      Add Prometheus metrics:
      - Sync success/failure counters
      - Response time histograms
      - DLQ size gauge
      - Metrics for each sync type
    </action>
    <verify>
      - Metrics defined using prometheus-client
      - Metrics exported via /metrics endpoint
      - Metrics incremented in sync operations
    </verify>
    <done>Prometheus metrics collection implemented</done>
  </task>

  <task type="auto" id="7">
    <name>Test ERP integration functionality</name>
    <files>tests/integrations/test_erp_sync.py</files>
    <action>
      Create integration tests:
      - Test ERPNext API client
      - Test sync operations
      - Test error handling
      - Mock ERPNext API responses
    </action>
    <verify>
      - Test file exists
      - Tests cover basic API operations
      - Tests for error handling and DLQ
    </verify>
    <done>ERP integration tests created</done>
  </task>
</tasks>

<verification>
- Run the ERP integration tests: pytest tests/integrations/test_erp_sync.py
- Verify all sync methods are implemented
- Check that webhook endpoint is created
- Confirm scheduler jobs are configured
- Ensure metrics are exported at /metrics
</verification>

<success_criteria>
- All tasks completed successfully
- Tests pass
- ERPNext API client can connect and fetch data
- Sync operations work correctly
- Webhook endpoint responds to events
- Scheduled tasks are running
- Metrics are available at /metrics
</success_criteria>

<output>
After completion, create SUMMARY.md with:
- Files created/modified
- Tasks completed
- Deviations from plan
- Metrics collected
- Any issues encountered
</output>
