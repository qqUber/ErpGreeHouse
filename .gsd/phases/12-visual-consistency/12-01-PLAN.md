---
phase: 12-visual-consistency
plan: 01
type: standard
---

<objective>
Establish visual consistency across all UI components and pages.

Purpose: Create a unified visual language with consistent colors, typography, spacing, and widget proportions.
Output: Visual consistency guidelines and fixed components.
</objective>

<context>
@.gsd/PROJECT.md
@.gsd/MILESTONE-CONTEXT.md
@admin-ui/src/index.css
@admin-ui/src/theme.ts
@admin-ui/src/components/
</context>

<tasks>
  <task type="auto">
    <name>Analyze current visual inconsistencies</name>
    <files>admin-ui/src/components/*.tsx</files>
    <action>
      1. Inventory all UI components
      2. Check color usage across components
      3. Analyze typography and spacing
      4. Identify inconsistent widget sizes
    </action>
    <verify>Component inventory and inconsistency report</verify>
    <done>Visual inconsistencies analyzed</done>
  </task>

  <task type="auto">
    <name>Create visual design system</name>
    <files>admin-ui/src/theme.ts, admin-ui/src/index.css</files>
    <action>
      1. Define color palette (primary, secondary, neutral)
      2. Establish typography hierarchy
      3. Create spacing scale
      4. Standardize widget sizes and proportions
    </action>
    <verify>Updated theme.ts with complete design system</verify>
    <done>Visual design system created</done>
  </task>

  <task type="auto">
    <name>Fix components visual consistency</name>
    <files>admin-ui/src/components/*.tsx</files>
    <action>
      1. Update all components with new design system
      2. Fix inconsistent colors and spacing
      3. Standardize widget shadows and borders
      4. Verify button styles, card designs, and inputs
    </action>
    <verify>All components use new design system</verify>
    <done>Components visually consistent</done>
  </task>

  <task type="checkpoint:human-verify" gate="blocking">
    <what-built>Visual design system and component fixes - dev server running</what-built>
    <how-to-verify>
      1. Start dev server: npm run dev in admin-ui
      2. Visit all pages: Dashboard, Customers, Products, Orders, Loyalty, Analytics
      3. Verify:
         - Consistent color usage across pages
         - Uniform spacing between elements
         - Standardized widget sizes
         - Cohesive typography hierarchy
      4. Check buttons, inputs, cards for consistency
    </how-to-verify>
    <resume-signal>Type "approved" or describe visual consistency issues</resume-signal>
  </task>
</tasks>

<verification>
Check all components for compliance with design system.
</verification>

<success_criteria>
- Visual design system created
- All components use consistent colors, typography, spacing
- Widget proportions standardized
- No inconsistent shadows, borders, or spacing
