---
phase: 09-hardening
plan: 01
type: standard
---

<objective>
Fix the 26 security issues identified in Phase 8 Security Audit. Focus on HIGH severity first, then review MEDIUM issues for false positives.

Purpose: Complete v1.1 Security milestone by hardening the codebase.
Output: Clean bandit report with all critical issues resolved.
</objective>

<context>
@.gsd/ROADMAP.md
@.gsd/phases/08-security-audit/08-01-PLAN.md
@.gsd/phases/08-security-audit/08-01-SUMMARY.md
@audit/bandit-results.json
@middleware/app/integrations/pos/erpnext_client.py
</context>

<must_haves>

- SHA1 hash replaced with SHA256 in erpnext_client.py
- SQL expression issues reviewed and documented
- Bandit report shows reduced severity issues
- All HIGH severity issues resolved

</must_haves>

<should_haves>

- LOW severity issues addressed or documented as acceptable
- Security tests added for key functions
- Clean bandit run with no HIGH issues

</should_haves>

<tasks>

<task type="auto">
  <name>Fix HIGH severity SHA1 hash</name>
  <files>middleware/app/integrations/pos/erpnext_client.py</files>
  <action>
    1. Replace hashlib.sha1 with hashlib.sha256 at line 261
    2. Verify the change doesn't break order_id generation
    3. Run bandit to confirm HIGH issue is resolved
  </action>
  <verify>
    - bandit report shows no HIGH issues for this file
    - order_id still generates correctly (10 char hex string)
  </verify>
  <done>SHA1 hash replaced with SHA256</done>
</task>

<task type="auto">
  <name>Review SQL expression issues</name>
  <files>middleware/app/handlers.py, middleware/app/integrations/bots/shared/commands.py</files>
  <action>
    1. Extract all 20 B608 flagged locations from bandit JSON
    2. Review each to confirm parameterized queries are used
    3. Add # noqa: B608 comments for false positives
    4. Fix any real SQL injection risks found
  </action>
  <verify>
    - All flagged locations reviewed
    - Parameterized queries confirmed for each
    - No real SQL injection vulnerabilities
  </verify>
  <done>SQL expression issues reviewed and documented</done>
</task>

<task type="auto">
  <name>Address LOW severity issues</name>
  <files>middleware/app/admin_api.py, middleware/app/admin_auth_api.py</files>
  <action>
    1. Review hardcoded string issues (cookie names)
    2. Review try/except/pass patterns
    3. Add noqa comments where appropriate or fix
  </action>
  <verify>
    - LOW issues addressed or documented
  </verify>
  <done>LOW severity issues addressed</done>
</task>

<task type="auto">
  <name>Verify clean security scan</name>
  <files>audit/bandit-report-final.html</files>
  <action>
    1. Run bandit on entire codebase
    2. Verify no HIGH severity issues remain
    3. Generate final report
  </action>
  <verify>
    - bandit shows 0 HIGH issues
    - All previous issues addressed
  </verify>
  <done>Security scan clean - no HIGH issues</done>
</task>

</tasks>

<verification>

Run bandit to verify fixes:

```bash
python -m bandit -r middleware/app -f html -o audit/bandit-report-final.html
python -m bandit -r middleware/app -f json -o audit/bandit-results-final.json
```

Verify: 
- HIGH: 0
- MEDIUM: < 20 (after false positive documentation)
- LOW: < 5

</verification>

<success_criteria>

- SHA1 replaced with SHA256
- All HIGH severity issues resolved
- SQL expression issues reviewed (false positives documented)
- Clean bandit report generated

</success_criteria>
