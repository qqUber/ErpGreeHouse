---
phase: 04-Compliance-&-Data-Protection
plan: 01
type: standard
---

<objective>
Enhance consent management for 152-FZ compliance. Improve consent collection, storage, and revocation processes.

Purpose: Ensure the system meets Russian data protection requirements for consent management.
Output: Enhanced consent management system with improved record-keeping and user controls.
</objective>

<context>
@.gsd/phases/04-Compliance-&-Data-Protection/04-CONTEXT.md
@middleware/app/integrations/bots/shared/consent.py
@middleware/app/db.py
@middleware/app/handlers.py
@middleware/app/integrations/bots/vk_handler.py
</context>

<success_criteria>

- Consent records include all required fields (timestamp, document version, consent type)
- Users can revoke consent and delete profile easily
- Consent management works across all channels (Telegram, VK)
- Compliance with 152-FZ requirements for consent collection and storage
- All tests pass

</success_criteria>

<tasks>

<task type="auto">
<name>Update consent management module</name>
<files>middleware/app/integrations/bots/shared/consent.py</files>
<action>
Enhance consent.py with:
1. More detailed consent text with policy version and effective date
2. Improved consent record-keeping with additional metadata
3. Function to revoke all consents for a user
4. Function to get consent history for audit purposes
</action>
<verify>
- File exists
- New functions are implemented
- No syntax errors
</verify>
<done>Consent management module updated</done>
</task>

<task type="auto">
<name>Enhance Telegram consent flow</name>
<files>middleware/app/handlers.py</files>
<action>
Improve Telegram bot consent handling:
1. Add /revoke_consent command to allow users to revoke marketing consent
2. Add /subscribe command to allow users to opt-in to marketing
3. Improve consent text with more detailed information
4. Add consent history check before sending marketing messages
</action>
<verify>
- File exists
- New commands are implemented
- Consent checks are added to message sending logic
</verify>
<done>Telegram consent flow enhanced</done>
</task>

<task type="auto">
<name>Enhance VK consent flow</name>
<files>middleware/app/integrations/bots/vk_handler.py</files>
<action>
Improve VK bot consent handling:
1. Add /delete command to allow users to delete their profile
2. Ensure consent checks are in place for VK messages
3. Improve consent text with more detailed information
4. Add consent history check before sending marketing messages
</action>
<verify>
- File exists
- New commands are implemented
- Consent checks are added to message sending logic
</verify>
<done>VK consent flow enhanced</done>
</task>

<task type="auto">
<name>Update database schema</name>
<files>middleware/app/db.py</files>
<action>
Update database schema for improved consent tracking:
1. Add additional fields to consents table (effective_date, ip_address)
2. Ensure consents table has proper indexes for fast queries
3. Add migration to update existing database
</action>
<verify>
- File exists
- Schema updates are implemented
- Migration logic is added
</verify>
<done>Database schema updated</done>
</task>

<task type="auto">
<name>Add compliance API endpoints</name>
<files>middleware/app/admin_api.py</files>
<action>
Add admin API endpoints for compliance management:
1. GET /api/v1/compliance/consents - Get consent records with filtering
2. GET /api/v1/compliance/consents/{customer_id} - Get consent history for a customer
3. DELETE /api/v1/compliance/customers/{customer_id} - Delete customer profile and all data
</action>
<verify>
- File exists
- New API endpoints are implemented
- Endpoints are properly authenticated
</verify>
<done>Compliance API endpoints added</done>
</task>

<task type="auto">
<name>Add compliance features to admin UI</name>
<files>admin-ui/src/pages/Compliance.tsx, admin-ui/src/components/ConsentTable.tsx, admin-ui/src/components/ProfileDeletion.tsx</files>
<action>
Create compliance management UI for admin users:
1. Consent records table with filtering and search
2. Customer profile deletion functionality
3. Consent history view for each customer
4. Export functionality for compliance reports
</action>
<verify>
- Files exist
- UI components are implemented
- Components are properly connected to API endpoints
</verify>
<done>Admin UI compliance features added</done>
</task>

<task type="auto">
<name>Write compliance tests</name>
<files>tests/unit/test_compliance.py</files>
<action>
Write comprehensive compliance tests:
1. Test consent collection and storage
2. Test consent revocation and deletion
3. Test compliance API endpoints
4. Test admin UI compliance features
</action>
<verify>
- File exists
- Tests are implemented
- All tests pass
</verify>
<done>Compliance tests written</done>
</task>

<task type="auto">
<name>Run compliance tests</name>
<action>Run all compliance tests to ensure everything works</action>
<verify>All compliance tests pass</verify>
<done>Compliance tests passed</done>
</task>

</tasks>

<verification>
Run all compliance tests:

```bash
cd middleware
python -m pytest tests/unit/test_compliance.py -v
```

Check API endpoints:

```bash
# Get consent records
curl -X GET "http://localhost:8000/api/v1/compliance/consents" -H "accept: application/json"

# Get customer consent history
curl -X GET "http://localhost:8000/api/v1/compliance/consents/{customer_id}" -H "accept: application/json"

# Delete customer profile
curl -X DELETE "http://localhost:8000/api/v1/compliance/customers/{customer_id}" -H "accept: application/json"
```
</verification>

<output>
After completion, create SUMMARY.md with:
- Changes to consent management module
- Enhancements to Telegram and VK consent flows
- Database schema updates
- New compliance API endpoints
- Admin UI compliance features
- Test coverage improvements
</output>