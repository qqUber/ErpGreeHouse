---
phase: 04-Compliance-&-Data-Protection
plan: 02
type: standard
---

<objective>
Implement profile deletion functionality for 152-FZ compliance. Ensure users can delete their profile with one click and all data is properly removed.

Purpose: Meet 152-FZ requirement for easy profile deletion and data removal.
Output: Complete profile deletion feature across all channels.
</objective>

<context>
@.gsd/phases/04-Compliance-&-Data-Protection/04-CONTEXT.md
@middleware/app/handlers.py
@middleware/app/integrations/bots/vk_handler.py
@middleware/app/integrations/bots/shared/consent.py
@middleware/app/db.py
</context>

<success_criteria>

- Users can delete their profile with one click from any supported channel
- All user data is properly removed from the system
- Compliance with 152-FZ requirements for data deletion
- Deletion process is auditable
- All tests pass

</success_criteria>

<tasks>

<task type="auto">
<name>Implement Telegram profile deletion</name>
<files>middleware/app/handlers.py</files>
<action>
Enhance Telegram bot with profile deletion functionality:
1. Improve /delete command with clearer confirmation
2. Add one-click deletion button to profile menu
3. Ensure all user data is properly removed
4. Add audit log entry for deletion
</action>
<verify>
- File exists
- Deletion functionality is implemented
- All user data is properly removed
- Audit log entry is created
</verify>
<done>Telegram profile deletion implemented</done>
</task>

<task type="auto">
<name>Implement VK profile deletion</name>
<files>middleware/app/integrations/bots/vk_handler.py</files>
<action>
Add profile deletion functionality to VK bot:
1. Add /delete command with confirmation
2. Add one-click deletion button to profile menu
3. Ensure all user data is properly removed
4. Add audit log entry for deletion
</action>
<verify>
- File exists
- Deletion functionality is implemented
- All user data is properly removed
- Audit log entry is created
</verify>
<done>VK profile deletion implemented</done>
</task>

<task type="auto">
<name>Enhance data removal logic</name>
<files>middleware/app/integrations/bots/shared/consent.py</files>
<action>
Improve data removal logic in consent.py:
1. Add function to delete all user data including consents, transactions, and other records
2. Ensure data removal is compliant with 152-FZ requirements
3. Add audit log entry for data removal
4. Improve error handling for deletion process
</action>
<verify>
- File exists
- Data removal function is implemented
- All user data is properly removed
- Audit log entry is created
- Error handling is improved
</verify>
<done>Data removal logic enhanced</done>
</task>

<task type="auto">
<name>Add deletion audit log</name>
<files>middleware/app/db.py</files>
<action>
Add audit log for profile deletion:
1. Create deletion_log table to track deletion events
2. Add migration to create the table
3. Ensure deletion events are logged with all relevant information
</action>
<verify>
- File exists
- deletion_log table is created
- Migration is implemented
- Deletion events are logged
</verify>
<done>Deletion audit log added</done>
</task>

<task type="auto">
<name>Add deletion API endpoint</name>
<files>middleware/app/admin_api.py</files>
<action>
Add admin API endpoint for profile deletion:
1. DELETE /api/v1/customers/{customer_id}/delete - Delete customer profile and all data
2. Add proper authentication and authorization
3. Ensure deletion process is auditable
</action>
<verify>
- File exists
- API endpoint is implemented
- Endpoint is properly authenticated
- Deletion process is auditable
</verify>
<done>Deletion API endpoint added</done>
</task>

<task type="auto">
<name>Add deletion UI to admin dashboard</name>
<files>admin-ui/src/pages/Customers.tsx, admin-ui/src/components/CustomerProfile.tsx</files>
<action>
Add profile deletion functionality to admin UI:
1. Add delete button to customer profile page
2. Add confirmation dialog before deletion
3. Show deletion history in customer profile
4. Ensure deletion process is auditable
</action>
<verify>
- Files exist
- Delete button is implemented
- Confirmation dialog is added
- Deletion history is shown
- Deletion process is auditable
</verify>
<done>Deletion UI added to admin dashboard</done>
</task>

<task type="auto">
<name>Write deletion tests</name>
<files>tests/unit/test_deletion.py</files>
<action>
Write comprehensive deletion tests:
1. Test profile deletion from Telegram
2. Test profile deletion from VK
3. Test deletion API endpoint
4. Test admin UI deletion functionality
5. Test data removal and audit logging
</action>
<verify>
- File exists
- Tests are implemented
- All tests pass
</verify>
<done>Deletion tests written</done>
</task>

<task type="auto">
<name>Run deletion tests</name>
<action>Run all deletion tests to ensure everything works</action>
<verify>All deletion tests pass</verify>
<done>Deletion tests passed</done>
</task>

</tasks>

<verification>
Run all deletion tests:

```bash
cd middleware
python -m pytest tests/unit/test_deletion.py -v
```

Check deletion API endpoint:

```bash
# Delete customer profile
curl -X DELETE "http://localhost:8000/api/v1/customers/{customer_id}/delete" -H "accept: application/json"
```

Test deletion from Telegram:
1. Send /delete command
2. Confirm deletion
3. Verify profile is deleted

Test deletion from VK:
1. Send /delete command
2. Confirm deletion
3. Verify profile is deleted
</verification>

<output>
After completion, create SUMMARY.md with:
- Profile deletion functionality across all channels
- Enhanced data removal logic
- Deletion audit log
- Deletion API endpoint
- Admin UI deletion features
- Test coverage improvements
</output>