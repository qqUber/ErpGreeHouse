---
phase: 00-test-improvement
plan: 02
type: execute
wave: 1
depends_on: [test-improvement-01]
files_modified:
  - middleware/app/integrations/bots/telegram_handler.py
  - middleware/tests/integration/test_telegram_integration.py
  - middleware/tests/unit/test_bot_handlers.py
  - middleware/app/handlers.py
  - middleware/app/integrations/bots/shared/commands.py
  - middleware/app/integrations/bots/vk_handler.py
  - middleware/tests/unit/test_vk_handler.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Telegram API calls are mocked in tests"
    - "Test coverage for handlers.py > 30%"
    - "Test coverage for commands.py > 20%"
    - "Test coverage for vk_handler.py > 20%"
  artifacts:
    - path: "middleware/tests/integration/test_telegram_integration.py"
      provides: "Mocked Telegram API integration tests"
      contains: "unittest.mock|pytest-mock"
    - path: "middleware/tests/unit/test_bot_handlers.py"
      provides: "Handlers test coverage"
      contains: "test_"
    - path: "middleware/tests/unit/test_commands.py"
      provides: "Commands test coverage"
      contains: "test_"
    - path: "middleware/tests/unit/test_vk_handler.py"
      provides: "VK handler test coverage"
      contains: "test_"
  key_links:
    - from: "middleware/tests/integration/test_telegram_integration.py"
      to: "middleware/app/integrations/bots/telegram_handler.py"
      via: "mocked API calls"
      pattern: "patch|mock"
---

<objective>
Mock Telegram API calls and improve test coverage for low coverage modules.

Purpose: Address flaky tests caused by real Telegram API calls and improve overall test coverage for critical modules.
Output: Mocked Telegram API integration tests and improved test coverage for handlers.py, commands.py, and vk_handler.py.
</objective>

<execution_context>
@~/.kilocode/skills/execute-plan/SKILL.md
@~/.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md

@middleware/app/integrations/bots/telegram_handler.py
@middleware/tests/integration/test_telegram_integration.py
@middleware/app/handlers.py
@middleware/app/integrations/bots/shared/commands.py
@middleware/app/integrations/bots/vk_handler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mock Telegram API calls for integration tests</name>
  <files>
    middleware/app/integrations/bots/telegram_handler.py
    middleware/tests/integration/test_telegram_integration.py
  </files>
  <action>
    Identify all places in telegram_handler.py that make real API calls to Telegram.
    Use unittest.mock or pytest-mock to patch these API calls in test_telegram_integration.py.
    Create mock responses that mimic the real Telegram API behavior.
  </action>
  <verify>
    Run the Telegram integration tests to verify they pass with mocked API calls:
    ```bash
    cd middleware && python -m pytest tests/integration/test_telegram_integration.py -v
    ```
  </verify>
  <done>
    Telegram integration tests pass with mocked API calls.
  </done>
</task>

<task type="auto">
  <name>Task 2: Improve test coverage for handlers.py</name>
  <files>
    middleware/app/handlers.py
    middleware/tests/unit/test_bot_handlers.py
  </files>
  <action>
    Analyze current test coverage for handlers.py (currently ~17%).
    Identify untested functions and edge cases.
    Write new tests in test_bot_handlers.py to cover these gaps.
    Focus on:
    - _store_consent function
    - Error handling scenarios
    - Edge cases in consent flow
  </action>
  <verify>
    Check test coverage for handlers.py:
    ```bash
    cd middleware && python -m pytest tests/unit/test_bot_handlers.py --cov=app.handlers --cov-report=term-missing
    ```
  </verify>
  <done>
    Test coverage for handlers.py > 30%.
  </done>
</task>

<task type="auto">
  <name>Task 3: Improve test coverage for commands.py</name>
  <files>
    middleware/app/integrations/bots/shared/commands.py
    middleware/tests/unit/test_commands.py
  </files>
  <action>
    Analyze current test coverage for commands.py (currently ~12%).
    Identify untested command handlers and edge cases.
    Write new tests in test_commands.py to cover these gaps.
  </action>
  <verify>
    Check test coverage for commands.py:
    ```bash
    cd middleware && python -m pytest tests/unit/test_commands.py --cov=app.integrations.bots.shared.commands --cov-report=term-missing
    ```
  </verify>
  <done>
    Test coverage for commands.py > 20%.
  </done>
</task>

<task type="auto">
  <name>Task 4: Improve test coverage for vk_handler.py</name>
  <files>
    middleware/app/integrations/bots/vk_handler.py
    middleware/tests/unit/test_vk_handler.py
  </files>
  <action>
    Analyze current test coverage for vk_handler.py (currently ~15%).
    Identify untested functions and edge cases.
    Write new tests in test_vk_handler.py to cover these gaps.
  </action>
  <verify>
    Check test coverage for vk_handler.py:
    ```bash
    cd middleware && python -m pytest tests/unit/test_vk_handler.py --cov=app.integrations.bots.vk_handler --cov-report=term-missing
    ```
  </verify>
  <done>
    Test coverage for vk_handler.py > 20%.
  </done>
</task>

</tasks>

<verification>
Run all tests to confirm coverage improvements:
```bash
cd middleware && python -m pytest tests/unit/test_bot_handlers.py tests/unit/test_commands.py tests/unit/test_vk_handler.py tests/integration/test_telegram_integration.py -v --cov=app.handlers --cov=app.integrations.bots.shared.commands --cov=app.integrations.bots.vk_handler --cov-report=term-missing
```
</verification>

<success_criteria>
- Telegram API calls are mocked in tests
- Test coverage for handlers.py > 30%
- Test coverage for commands.py > 20%
- Test coverage for vk_handler.py > 20%
- All tests pass
</success_criteria>

<output>
After completion, create `.gsd/phases/test-improvement/test-improvement-02-SUMMARY.md`
</output>
