---
phase: 00-test-improvement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - middleware/app/handlers.py
  - middleware/tests/unit/test_consent_flow.py
  - middleware/tests/conftest.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Consent flow tests pass"
    - "Database file locking issue resolved"
  artifacts:
    - path: "middleware/app/handlers.py"
      provides: "_store_consent function with correct signature"
      contains: "def _store_consent"
    - path: "middleware/tests/unit/test_consent_flow.py"
      provides: "Test for consent flow"
      contains: "test_store_consent_with_type"
    - path: "middleware/tests/conftest.py"
      provides: "Database fixture without file locking"
      contains: "clean_database"
  key_links:
    - from: "middleware/tests/unit/test_consent_flow.py"
      to: "middleware/app/handlers.py"
      via: "test calls _store_consent"
      pattern: "_store_consent"
---

<objective>
Fix critical test failures in consent flow and database locking.

Purpose: These failures are blocking the test suite from running successfully, which hinders development and deployment.
Output: Fixed test failures, allowing the test suite to run without errors.
</objective>

<execution_context>
@~/.kilocode/skills/execute-plan/SKILL.md
@~/.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md

@middleware/app/handlers.py
@middleware/tests/unit/test_consent_flow.py
@middleware/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix consent flow test - _store_consent() function signature</name>
  <files>
    middleware/app/handlers.py
    middleware/tests/unit/test_consent_flow.py
  </files>
  <action>
    Check the current signature of `_store_consent()` in handlers.py (line 212):
    ```python
    def _store_consent(customer_id: int, consent_text: str, consent_version: str) -> None:
    ```
    
    Check how it's being called in the test `test_store_consent_with_type` in test_consent_flow.py.
    The error indicates it's being called with 4 positional arguments. Fix either the function signature to accept the additional parameter or the test to pass the correct number of arguments.
  </action>
  <verify>
    Run the consent flow test specifically:
    ```bash
    cd middleware && python -m pytest tests/unit/test_consent_flow.py::test_store_consent_with_type -v
    ```
  </verify>
  <done>
    Test `test_store_consent_with_type` passes without TypeError.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix database file locking issue in tests</name>
  <files>middleware/tests/conftest.py</files>
  <action>
    The database file locking issue occurs in the `clean_database` fixture at line 123 on Windows.
    The error is: "PermissionError: [WinError 32] The process cannot access the file because it is being used by another process: 'test_telegram_crm.db'"
    
    Fix the fixture to properly handle database connections and ensure files are closed before deletion.
    Possible solutions:
    1. Ensure all connections are closed before attempting to delete the file
    2. Add retry logic with delays
    3. Use a different approach to cleaning up the database
  </action>
  <verify>
    Run the tests that use the clean_database fixture to verify the fix:
    ```bash
    cd middleware && python -m pytest tests/unit/test_consent_flow.py -v
    ```
  </verify>
  <done>
    Tests using the clean_database fixture run without PermissionErrors.
  </done>
</task>

</tasks>

<verification>
Run the entire test suite to confirm fixes:
```bash
cd middleware && python -m pytest -v
```
</verification>

<success_criteria>
- Consent flow tests pass
- Database file locking issue resolved
- All tests run without errors
</success_criteria>

<output>
After completion, create `.gsd/phases/test-improvement/test-improvement-01-SUMMARY.md`
</output>
