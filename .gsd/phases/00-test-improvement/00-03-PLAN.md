---
phase: 00-test-improvement
plan: 03
type: execute
wave: 2
depends_on: [test-improvement-01, test-improvement-02]
files_modified: []
autonomous: false
gap_closure: true
must_haves:
  truths:
    - "Cross-phase integration (API → UI → Database) works"
    - "Full test suite passes"
  artifacts: []
  key_links: []
---

<objective>
Verify cross-phase integration and run full test suite to confirm all fixes.

Purpose: Ensure that all components work together and that our fixes didn't break any existing functionality.
Output: Verified cross-phase integration and passing full test suite.
</objective>

<execution_context>
@~/.kilocode/skills/execute-plan/SKILL.md
@~/.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Verify cross-phase integration (API → UI → Database)</name>
  <action>
    Run the admin UI and verify that the API → UI → Database integration works correctly.
  </action>
  <instructions>
    To verify the cross-phase integration:
    1. Start the middleware server (backend API)
    2. Start the admin UI
    3. Navigate through the UI and perform actions that interact with the API and database
    4. Verify that changes are reflected in the database

    This step requires manual verification as it involves the complete system integration.
  </instructions>
  <verification>
    Check that UI actions are reflected in the database:
    - Create a new user in the UI
    - Verify the user exists in the database
    - Update user information in the UI
    - Verify the changes are reflected in the database
  </verification>
  <resume-signal>Type "approved" if the integration works, or describe any issues you found</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run full test suite to confirm fixes</name>
  <files>[]</files>
  <action>
    Run the entire test suite to confirm that all fixes have been applied correctly and that no new failures are introduced.
  </action>
  <verify>
    Run the full test suite with coverage:
    ```bash
    cd middleware && python -m pytest -v --tb=short
    ```
  </verify>
  <done>
    All tests pass.
  </done>
</task>

</tasks>

<verification>
Run full test suite to confirm all fixes:
```bash
cd middleware && python -m pytest -v --tb=short
```
</verification>

<success_criteria>
- Cross-phase integration (API → UI → Database) works
- Full test suite passes
- All critical failures (consent flow, database locking, Telegram integration) are fixed
- Test coverage improved for low coverage areas
</success_criteria>

<output>
After completion, create `.gsd/phases/test-improvement/test-improvement-03-SUMMARY.md`
</output>
