---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - middleware/app/db.py
  - middleware/seed_data.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "SQLite database is initialized with all required tables"
    - "Database connection is properly configured and testable"
    - "Initial seed data is loaded into the database"
  artifacts:
    - path: "middleware/app/db.py"
      provides: "Database connection and schema management"
      contains: "init_db|get_db|SQLite|Table"
    - path: "middleware/seed_data.py"
      provides: "Initial data seeding"
      contains: "seed|load_data|insert"
  key_links:
    - from: "middleware/app/db.py"
      to: "middleware/.env"
      via: "database path from environment variable"
      pattern: "CRM_DB_PATH"
---

<objective>
Set up the SQLite database with initial schema and seed data for ErpGreeHouse.

Purpose: Provide a working database with all necessary tables and initial data for development and testing.
Output: Initialized SQLite database with required tables and seed data.
</objective>

<execution_context>
@~/.kilocode/skills/execute-plan/SKILL.md
@~/.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@middleware/app/db.py
@middleware/seed_data.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize the database schema</name>
  <files>middleware/app/db.py</files>
  <action>
    Run the database initialization script to create all required tables.
    Verify that the CRM database is properly initialized by checking for the existence of tables.
  </action>
  <verify>
    python -c "from app.db import init_db; init_db()" && python -c "from app.db import get_db; db = next(get_db()); cursor = db.cursor(); cursor.execute('SELECT name FROM sqlite_master WHERE type=\"table\"'); tables = [t[0] for t in cursor.fetchall()]; print('Tables:', tables)"
  </verify>
  <done>Database schema is initialized with all required tables</done>
</task>

<task type="auto">
  <name>Task 2: Load initial seed data</name>
  <files>middleware/seed_data.py</files>
  <action>
    Run the seed data script to populate the database with initial data.
    Verify that seed data is properly loaded by checking for existing records.
  </action>
  <verify>
    python -m middleware.seed_data && python -c "from app.db import get_db; db = next(get_db()); cursor = db.cursor(); cursor.execute('SELECT COUNT(*) FROM users'); print('Users count:', cursor.fetchone()[0]); cursor.execute('SELECT COUNT(*) FROM loyalty_tiers'); print('Loyalty tiers count:', cursor.fetchone()[0])"
  </verify>
  <done>Initial seed data is loaded into the database</done>
</task>

<task type="auto">
  <name>Task 3: Verify database connectivity</name>
  <files>middleware/app/db.py</files>
  <action>
    Create a simple test to verify database connectivity.
    Test that we can connect to the database, execute queries, and retrieve data.
  </action>
  <verify>
    python -c "
from app.db import get_db
try:
    db = next(get_db())
    cursor = db.cursor()
    cursor.execute('SELECT 1')
    result = cursor.fetchone()
    print('Database connection successful:', result == (1,))
except Exception as e:
    print('Database connection failed:', e)
"
  </verify>
  <done>Database connectivity is verified</done>
</task>

</tasks>

<verification>
- Database schema is initialized
- Seed data is loaded
- Database connection is testable
- All required tables exist
</verification>

<success_criteria>
- python -c "from app.db import init_db; init_db()" executes without errors
- python -m middleware.seed_data executes without errors
- Database has at least one user and loyalty tier record
- Database connection test returns "Database connection successful: True"
</success_criteria>

<output>
After completion, create `.gsd/phases/01-foundation/01-02-SUMMARY.md`
</output>
