---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - middleware/.env
  - middleware/requirements.txt
  - middleware/app/config.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Development environment is properly configured with all dependencies installed"
    - "Environment variables are set up for local development"
    - "Configuration file is correctly parsing environment variables"
  artifacts:
    - path: "middleware/.env"
      provides: "Environment variable definitions"
      contains: "CRM_DB_PATH|JWT_SECRET|ERP_BASE_URL|ERP_API_KEY"
    - path: "middleware/requirements.txt"
      provides: "Project dependencies"
      contains: "fastapi|uvicorn|aiogram|celery|python-jose|passlib"
    - path: "middleware/app/config.py"
      provides: "Configuration management"
      contains: "class Settings|@lru_cache|env_file"
  key_links:
    - from: "middleware/app/config.py"
      to: "middleware/.env"
      via: "pydantic env file reading"
      pattern: "env_file.*\\.env"
---

<objective>
Set up the development environment for ErpGreeHouse, including dependency installation, environment variable configuration, and initial project setup.

Purpose: Ensure the application can be run locally with all necessary dependencies and configurations.
Output: Configured development environment with all dependencies installed.
</objective>

<execution_context>
@~/.kilocode/skills/execute-plan/SKILL.md
@~/.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@middleware/requirements.txt
@middleware/app/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install project dependencies</name>
  <files>middleware/requirements.txt</files>
  <action>
    Run `pip install -r middleware/requirements.txt` to install all necessary Python dependencies.
    Verify installation by checking if key packages (fastapi, uvicorn, aiogram, celery) are installed.
  </action>
  <verify>
    pip list | grep -E "fastapi|uvicorn|aiogram|celery|python-jose|passlib"
  </verify>
  <done>All project dependencies are installed</done>
</task>

<task type="auto">
  <name>Task 2: Configure environment variables</name>
  <files>middleware/.env</files>
  <action>
    Create or update the .env file with the following variables:
    - CRM_DB_PATH: Path to SQLite database file
    - JWT_SECRET: Secret key for JWT token generation
    - ERP_BASE_URL: Base URL for ERPNext integration (use mock endpoint for development)
    - ERP_API_KEY: API key for ERPNext integration (use dummy value for development)
    - REDIS_URL: URL for Redis broker (use local Redis instance)
  </action>
  <verify>
    cat middleware/.env | grep -E "CRM_DB_PATH|JWT_SECRET|ERP_BASE_URL|ERP_API_KEY|REDIS_URL"
  </verify>
  <done>Environment variables are properly configured</done>
</task>

<task type="auto">
  <name>Task 3: Verify configuration file</name>
  <files>middleware/app/config.py</files>
  <action>
    Ensure the configuration file is correctly parsing environment variables using pydantic Settings class.
    Verify that all required variables are defined and have appropriate default values.
  </action>
  <verify>
    python -c "from app.config import Settings; s = Settings(); print('Configuration loaded successfully')"
  </verify>
  <done>Configuration file is working correctly</done>
</task>

</tasks>

<verification>
- All dependencies are installed
- Environment variables are configured
- Configuration file loads successfully
- Application can start without errors
</verification>

<success_criteria>
- pip list shows all required dependencies
- .env file contains all required variables
- python -c "from app.config import Settings; s = Settings()" executes without errors
</success_criteria>

<output>
After completion, create `.gsd/phases/01-foundation/01-01-SUMMARY.md`
</output>
