---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - middleware/run_tests.sh
  - middleware/run_tests.ps1
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All tests are passing successfully"
    - "Test execution commands are working correctly"
    - "Test coverage includes all critical functionality"
  artifacts:
    - path: "middleware/tests/"
      provides: "Test suite"
      contains: "test_*.py"
  key_links:
    - from: "middleware/run_tests.sh"
      to: "middleware/tests/"
      via: "test discovery and execution"
      pattern: "pytest|python.*-m.*test"
---

<objective>
Run the complete test suite to verify that all tests are passing and the application is functioning correctly.

Purpose: Ensure that all tests pass and the application is in a stable state.
Output: Test report showing all tests passing.
</objective>

<execution_context>
@~/.kilocode/skills/execute-plan/SKILL.md
@~/.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@middleware/run_tests.sh
@middleware/run_tests.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all tests using shell script</name>
  <files>middleware/run_tests.sh</files>
  <action>
    Run the complete test suite using the shell script.
    Command: `cd middleware && ./run_tests.sh`
  </action>
  <verify>
    cd middleware && ./run_tests.sh 2>&1 | grep -E "All tests passed|100%|passed"
  </verify>
  <done>All tests pass using the shell script</done>
</task>

<task type="auto">
  <name>Task 2: Run all tests using PowerShell script</name>
  <files>middleware/run_tests.ps1</files>
  <action>
    Run the complete test suite using the PowerShell script.
    Command: `cd middleware && powershell -ExecutionPolicy Bypass -File run_tests.ps1`
  </action>
  <verify>
    cd middleware && powershell -ExecutionPolicy Bypass -File run_tests.ps1 2>&1 | grep -E "All tests passed|100%|passed"
  </verify>
  <done>All tests pass using the PowerShell script</done>
</task>

<task type="auto">
  <name>Task 3: Run tests directly with pytest</name>
  <files>middleware/tests/</files>
  <action>
    Run the test suite directly with pytest.
    Command: `cd middleware && python -m pytest tests/ -v`
  </action>
  <verify>
    cd middleware && python -m pytest tests/ -v 2>&1 | grep -E "passed|100%"
  </verify>
  <done>All tests pass when run directly with pytest</done>
</task>

</tasks>

<verification>
- All tests pass using both shell and PowerShell scripts
- Tests can be run directly with pytest
- Test coverage includes all critical functionality
- No errors or failures in the test results
</verification>

<success_criteria>
- ./run_tests.sh completes with "All tests passed" or 100% passed
- run_tests.ps1 completes with "All tests passed" or 100% passed
- python -m pytest tests/ -v shows all tests passing
- No test failures or errors are reported
</success_criteria>

<output>
After completion, create `.gsd/phases/01-foundation/01-04-SUMMARY.md`
</output>
